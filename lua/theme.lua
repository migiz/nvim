-- Single switch for the active colorscheme across the whole config.
-- Both colorschemes stay installed; flip this string to toggle.
-- Valid values: "everforest", "catppuccin" (must match a loaded colorscheme name).
return "everforest"
